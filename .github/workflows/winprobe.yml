name: winprobe
on: push
permissions:
  contents: read
jobs:
  probe:
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    env:
      MSYS: winsymlinks:nativestrict
    steps:
      - uses: actions/checkout@v4
      - name: probe
        run: bash tests/_winprobe.sh
