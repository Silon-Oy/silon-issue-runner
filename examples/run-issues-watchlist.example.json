{
  "_comment": "Example watchlist for poller.sh / pr-watch-poller.sh. Copy to a private location (the pollers read $HOME/dotfiles/machine-studio/run-issues-watchlist.json) and replace every <placeholder>. JSON has no comment syntax — the _comment key is the convention used instead.",
  "default_labels": ["auto-run"],
  "global_max_concurrent": 2,
  "repos": [
    {
      "path": "/Users/<you>/Projects/<repo>",
      "labels": ["auto-run"],
      "remotes": ["origin"]
    },
    {
      "path": "/Users/<you>/Projects/<fork-repo>",
      "labels": ["auto-run", "auto-clean"],
      "remotes": ["origin", "upstream"]
    }
  ]
}
